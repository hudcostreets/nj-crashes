# DVX-tracked data dirs (provenance via .dvc files at sibling paths)
raw/h3_r*/
raw/s2_l*/
pyramid/
s2_pyramid/
cells.db
cells-s2.db

# Regenerated by `njdot compute cells manifest` from on-disk shards
manifest.json
